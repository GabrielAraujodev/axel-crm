import { Routes } from '@angular/router';
import { DocumentsListComponent } from './documents-list.component';
export const DOCUMENTS_ROUTES: Routes = [{ path: '', component: DocumentsListComponent }];
