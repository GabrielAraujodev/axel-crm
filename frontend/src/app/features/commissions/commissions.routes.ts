import { Routes } from '@angular/router'; import { CommissionsListComponent } from './commissions-list.component';
export const COMMISSIONS_ROUTES: Routes = [{ path: '', component: CommissionsListComponent }];
