import { Routes } from '@angular/router';
import { LegalProcessesListComponent } from './legal-processes-list.component';

export const LEGAL_PROCESSES_ROUTES: Routes = [
  { path: '', component: LegalProcessesListComponent },
];
