import { Routes } from '@angular/router';
import { ContractsListComponent } from './contracts-list.component';
export const CONTRACTS_ROUTES: Routes = [{ path: '', component: ContractsListComponent }];
